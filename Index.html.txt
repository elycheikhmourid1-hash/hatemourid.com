<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ely Cheikh Mourid - AI Expert & Entrepreneur</title>
    <meta name="description" content="AI Data Annotator & Entrepreneur specializing in intelligent systems and digital solutions. Owner of hatemourid.com">
    <meta name="keywords" content="AI Expert, Data Annotation, Machine Learning, hatemourid.com, Ely Cheikh Mourid">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        /* ===== VARIABLES ===== */
        :root {
            --primary: #00d4ff;
            --secondary: #ff0080;
            --success: #00ffaa;
            --dark: #0a0e17;
            --darker: #050811;
            --light: #f0f8ff;
        }
        
        /* ===== RESET ===== */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        html {
            scroll-behavior: smooth;
        }
        
        body {
            font-family: 'Poppins', sans-serif;
            background: var(--dark);
            color: var(--light);
            overflow-x: hidden;
            min-height: 100vh;
        }
        
        /* ===== LOADING SCREEN ===== */
        #loading {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--darker);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 9999;
            transition: opacity 0.5s ease;
        }
        
        .ai-loader {
            text-align: center;
        }
        
        .loader-circle {
            width: 80px;
            height: 80px;
            border: 5px solid rgba(0, 212, 255, 0.2);
            border-top: 5px solid var(--primary);
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin: 0 auto 20px;
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        /* ===== HEADER ===== */
        .header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            background: rgba(10, 14, 23, 0.97);
            backdrop-filter: blur(10px);
            padding: 15px 30px;
            z-index: 1000;
            border-bottom: 1px solid rgba(0, 212, 255, 0.2);
            box-shadow: 0 5px 30px rgba(0, 0, 0, 0.5);
        }
        
        .nav-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1400px;
            margin: 0 auto;
        }
        
        .logo {
            display: flex;
            align-items: center;
            gap: 15px;
        }
        
        .logo-icon {
            width: 60px;
            height: 60px;
            background: linear-gradient(45deg, var(--primary), var(--secondary));
            border-radius: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 28px;
            font-weight: 900;
            color: white;
            animation: logoGlow 2s infinite alternate;
        }
        
        @keyframes logoGlow {
            0% { box-shadow: 0 0 10px var(--primary); }
            100% { box-shadow: 0 0 30px var(--secondary); }
        }
        
        .logo-text h1 {
            font-size: 24px;
            background: linear-gradient(to right, var(--primary), var(--success));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            font-weight: 900;
            letter-spacing: 1px;
        }
        
        .logo-text p {
            color: #aaa;
            font-size: 12px;
            letter-spacing: 2px;
        }
        
        .nav-menu {
            display: flex;
            gap: 30px;
            align-items: center;
        }
        
        .nav-link {
            color: #ccc;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
            position: relative;
        }
        
        .nav-link:hover {
            color: var(--primary);
        }
        
        .nav-link.active {
            color: var(--primary);
        }
        
        .nav-link.active::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 100%;
            height: 2px;
            background: var(--primary);
            border-radius: 2px;
        }
        
        /* ===== DOMAIN BANNER ===== */
        .domain-banner {
            background: linear-gradient(45deg, var(--secondary), var(--primary));
            color: white;
            text-align: center;
            padding: 10px;
            font-weight: 600;
            position: relative;
            overflow: hidden;
        }
        
        .domain-banner .domain-link {
            color: white;
            text-decoration: none;
            font-weight: 700;
            margin-left: 5px;
        }
        
        .domain-banner .domain-link:hover {
            text-decoration: underline;
        }
        
        /* ===== PROFILE HERO ===== */
        .profile-hero {
            min-height: 100vh;
            padding: 180px 30px 100px;
            position: relative;
            overflow: hidden;
            display: flex;
            align-items: center;
            background: linear-gradient(135deg, rgba(10, 14, 23, 0.9) 0%, rgba(5, 8, 17, 0.9) 100%);
            background-size: cover;
            background-position: center;
            background-blend-mode: overlay;
        }
        
        .profile-container {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
            align-items: center;
        }
        
        .profile-image-container {
            position: relative;
            text-align: center;
        }
        
        .profile-image {
            width: 320px;
            height: 320px;
            border-radius: 50%;
            border: 5px solid transparent;
            background: linear-gradient(45deg, var(--primary), var(--secondary), var(--success)) border-box;
            -webkit-mask: linear-gradient(#fff 0 0) padding-box, linear-gradient(#fff 0 0);
            -webkit-mask-composite: xor;
            mask-composite: exclude;
            object-fit: cover;
            animation: profileGlow 3s infinite alternate;
            box-shadow: 0 20px 60px rgba(0, 212, 255, 0.3);
        }
        
        @keyframes profileGlow {
            0% { box-shadow: 0 0 30px rgba(0, 212, 255, 0.3); }
            100% { box-shadow: 0 0 60px rgba(255, 0, 128, 0.5); }
        }
        
        .profile-badge {
            position: absolute;
            bottom: 30px;
            right: 30px;
            background: linear-gradient(45deg, var(--secondary), var(--primary));
            color: white;
            padding: 12px 25px;
            border-radius: 50px;
            font-weight: 700;
            animation: badgeFloat 3s infinite ease-in-out;
        }
        
        @keyframes badgeFloat {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }
        
        .profile-info {
            padding: 40px;
        }
        
        .profile-title {
            font-size: 16px;
            color: var(--primary);
            text-transform: uppercase;
            letter-spacing: 3px;
            margin-bottom: 15px;
        }
        
        .profile-name {
            font-size: 60px;
            line-height: 1.1;
            margin-bottom: 20px;
            background: linear-gradient(to right, var(--primary), var(--secondary), var(--success));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            font-weight: 900;
        }
        
        .profile-role {
            font-size: 24px;
            color: #bbb;
            margin-bottom: 30px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .profile-role i {
            color: var(--success);
        }
        
        .profile-description {
            font-size: 18px;
            line-height: 1.8;
            color: #ccc;
            margin-bottom: 40px;
        }
        
        .profile-contact {
            display: flex;
            gap: 20px;
            flex-wrap: wrap;
        }
        
        .contact-item {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 15px 25px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 15px;
            border: 1px solid rgba(0, 212, 255, 0.2);
            transition: all 0.3s ease;
        }
        
        .contact-item:hover {
            background: rgba(0, 212, 255, 0.1);
            transform: translateY(-5px);
        }
        
        .contact-item i {
            color: var(--primary);
            font-size: 20px;
        }
        
        /* ===== DOMAINS SECTION ===== */
        .domains-section {
            padding: 100px 30px;
            background: rgba(5, 8, 17, 0.8);
        }
        
        .section-title {
            text-align: center;
            font-size: 42px;
            margin-bottom: 60px;
            background: linear-gradient(to right, var(--primary), var(--success));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }
        
        .domains-grid {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 30px;
        }
        
        .domain-card {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 20px;
            padding: 40px;
            border: 1px solid rgba(0, 212, 255, 0.1);
            transition: all 0.3s ease;
            text-align: center;
            position: relative;
        }
        
        .domain-card.featured {
            border: 2px solid var(--primary);
            box-shadow: 0 0 30px rgba(0, 212, 255, 0.3);
        }
        
        .domain-card.featured::before {
            content: 'PRIMARY DOMAIN';
            position: absolute;
            top: -12px;
            left: 50%;
            transform: translateX(-50%);
            background: var(--primary);
            color: var(--dark);
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 700;
        }
        
        .domain-card:hover {
            transform: translateY(-10px);
            border-color: var(--primary);
            box-shadow: 0 20px 40px rgba(0, 212, 255, 0.2);
        }
        
        .domain-icon {
            width: 80px;
            height: 80px;
            background: linear-gradient(45deg, var(--primary), var(--secondary));
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 36px;
            margin: 0 auto 25px;
            color: white;
        }
        
        .domain-card h3 {
            font-size: 24px;
            margin-bottom: 15px;
            color: white;
        }
        
        .domain-url {
            color: var(--primary);
            font-size: 18px;
            text-decoration: none;
            display: block;
            margin: 15px 0;
            font-weight: 600;
            word-break: break-all;
        }
        
        .domain-url:hover {
            text-decoration: underline;
        }
        
        .domain-description {
            color: #aaa;
            line-height: 1.6;
        }
        
        /* ===== SKILLS SECTION ===== */
        .skills-section {
            padding: 100px 30px;
        }
        
        .skills-container {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
        }
        
        .skill-card {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 20px;
            padding: 30px;
            border: 1px solid rgba(0, 212, 255, 0.1);
            transition: all 0.3s ease;
        }
        
        .skill-card:hover {
            transform: translateY(-5px);
            border-color: var(--primary);
        }
        
        .skill-header {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-bottom: 20px;
        }
        
        .skill-header i {
            color: var(--primary);
            font-size: 30px;
        }
        
        .skill-name {
            font-size: 20px;
            color: white;
        }
        
        .skill-level {
            height: 8px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 4px;
            overflow: hidden;
            margin-top: 10px;
        }
        
        .skill-progress {
            height: 100%;
            background: linear-gradient(to right, var(--primary), var(--success));
            border-radius: 4px;
            width: 0;
            transition: width 1.5s ease;
        }
        
        /* ===== AI CHAT SYSTEM ===== */
        .ai-chat-container {
            position: fixed;
            bottom: 120px;
            right: 30px;
            width: 400px;
            height: 600px;
            background: rgba(15, 20, 35, 0.98);
            backdrop-filter: blur(20px);
            border-radius: 25px;
            border: 1px solid rgba(0, 212, 255, 0.3);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.7);
            z-index: 1001;
            display: none;
            flex-direction: column;
            overflow: hidden;
        }
        
        .chat-header {
            padding: 20px;
            background: linear-gradient(45deg, rgba(0, 212, 255, 0.1), rgba(255, 0, 128, 0.1));
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .chat-header h3 {
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 18px;
        }
        
        .chat-body {
            flex: 1;
            padding: 20px;
            overflow-y: auto;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }
        
        .message {
            max-width: 80%;
            padding: 15px;
            border-radius: 18px;
            position: relative;
        }
        
        .message.ai {
            align-self: flex-start;
            background: rgba(0, 212, 255, 0.1);
            border: 1px solid rgba(0, 212, 255, 0.3);
        }
        
        .message.user {
            align-self: flex-end;
            background: rgba(255, 0, 128, 0.1);
            border: 1px solid rgba(255, 0, 128, 0.3);
        }
        
        /* ===== AI CHAT BUTTON ===== */
        .ai-chat-btn {
            position: fixed;
            bottom: 40px;
            right: 40px;
            width: 80px;
            height: 80px;
            border-radius: 50%;
            background: linear-gradient(45deg, var(--secondary), var(--primary));
            border: none;
            color: white;
            font-size: 32px;
            cursor: pointer;
            box-shadow: 0 10px 30px rgba(255, 0, 128, 0.5);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 1000;
            transition: all 0.3s ease;
        }
        
        .ai-chat-btn:hover {
            transform: scale(1.1) rotate(15deg);
            box-shadow: 0 15px 40px rgba(0, 212, 255, 0.6);
        }
        
        /* ===== FOOTER ===== */
        .footer {
            background: rgba(5, 8, 17, 0.98);
            padding: 60px 30px 30px;
            border-top: 1px solid rgba(0, 212, 255, 0.2);
        }
        
        .footer-grid {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
        }
        
        .footer-section h3 {
            color: var(--primary);
            margin-bottom: 25px;
            font-size: 20px;
        }
        
        .footer-links {
            list-style: none;
        }
        
        .footer-links li {
            margin-bottom: 12px;
        }
        
        .footer-links a {
            color: #aaa;
            text-decoration: none;
            transition: color 0.3s ease;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .footer-links a:hover {
            color: var(--primary);
        }
        
        .copyright {
            text-align: center;
            margin-top: 50px;
            padding-top: 30px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            color: #777;
            font-size: 14px;
        }
        
        /* ===== BUTTONS ===== */
        .btn {
            padding: 15px 30px;
            border-radius: 25px;
            font-size: 16px;
            font-weight: 600;
            border: none;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            transition: all 0.3s ease;
            text-decoration: none;
        }
        
        .btn-primary {
            background: linear-gradient(45deg, var(--primary), var(--secondary));
            color: white;
        }
        
        .btn-secondary {
            background: transparent;
            color: var(--primary);
            border: 2px solid var(--primary);
        }
        
        .btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0, 212, 255, 0.3);
        }
        
        /* ===== RESPONSIVE ===== */
        @media (max-width: 1200px) {
            .profile-container {
                grid-template-columns: 1fr;
                text-align: center;
            }
            
            .profile-image {
                width: 280px;
                height: 280px;
            }
            
            .profile-name {
                font-size: 48px;
            }
        }
        
        @media (max-width: 768px) {
            .nav-menu {
                display: none;
            }
            
            .profile-hero {
                padding: 150px 20px 80px;
            }
            
            .profile-name {
                font-size: 36px;
            }
            
            .profile-role {
                font-size: 20px;
            }
            
            .ai-chat-container {
                width: 90%;
                right: 5%;
                height: 500px;
            }
            
            .contact-item {
                width: 100%;
                justify-content: center;
            }
            
            .domains-grid {
                grid-template-columns: 1fr;
            }
        }
        
        @media (max-width: 480px) {
            .profile-name {
                font-size: 32px;
            }
            
            .profile-image {
                width: 240px;
                height: 240px;
            }
            
            .ai-chat-btn {
                width: 60px;
                height: 60px;
                font-size: 24px;
                bottom: 20px;
                right: 20px;
            }
            
            .section-title {
                font-size: 32px;
            }
        }
    </style>
</head>
<body>
    <!-- Loading Screen -->
    <div id="loading">
        <div class="ai-loader">
            <div class="loader-circle"></div>
            <h3>Loading Professional Portfolio...</h3>
            <p style="color: #aaa; margin-top: 10px;">Ely Cheikh Mourid - AI Expert</p>
        </div>
    </div>

    <!-- Domain Banner -->
    <div class="domain-banner">
        <i class="fas fa-globe"></i> Official Website: 
        <a href="https://hatemourid.com" target="_blank" class="domain-link">hatemourid.com</a>
        | AI Expert Portfolio
    </div>

    <!-- Header -->
    <header class="header">
        <div class="nav-container">
            <div class="logo">
                <div class="logo-icon">EM</div>
                <div class="logo-text">
                    <h1>ELY CHEIKH MOURID</h1>
                    <p>AI EXPERT & ENTREPRENEUR</p>
                </div>
            </div>
            
            <div class="nav-menu">
                <a href="#home" class="nav-link active">Home</a>
                <a href="#domains" class="nav-link">Domains</a>
                <a href="#skills" class="nav-link">Skills</a>
                <a href="#stores" class="nav-link">Stores</a>
                <a href="#contact" class="nav-link">Contact</a>
            </div>
        </div>
    </header>

    <!-- Profile Hero Section -->
    <section id="home" class="profile-hero">
        <div class="profile-container">
            <div class="profile-image-container">
                <img src="https://i.ibb.co/Vc1yDmjc/IMG-5907.png" 
                     alt="Ely Cheikh Mourid" class="profile-image">
                <div class="profile-badge">
                    <i class="fas fa-star"></i> AI Expert
                </div>
            </div>
            
            <div class="profile-info">
                <div class="profile-title">AI & Data Science Professional</div>
                <h1 class="profile-name">Ely Cheikh Mourid</h1>
                
                <div class="profile-role">
                    <i class="fas fa-briefcase"></i>
                    <span>AI Data Annotator • Remote Freelancer • Entrepreneur</span>
                </div>
                
                <p class="profile-description">
                    Specializing in AI/ML Data Annotation, Labeling, and Evaluation. 
                    Building innovative digital solutions through AI-powered platforms 
                    and e-commerce systems. Owner and developer of 
                    <strong>hatemourid.com</strong> - my primary professional domain 
                    featuring cutting-edge AI solutions and digital services.
                </p>
                
                <div class="profile-contact">
                    <div class="contact-item">
                        <i class="fas fa-phone-alt"></i>
                        <span>+1 (804) 485-3384</span>
                    </div>
                    
                    <div class="contact-item">
                        <i class="fas fa-envelope"></i>
                        <span>elycheikhmourid1@gmail.com</span>
                    </div>
                    
                    <div class="contact-item">
                        <i class="fas fa-globe"></i>
                        <span>hatemourid.com</span>
                    </div>
                </div>
                
                <div style="margin-top: 40px; display: flex; gap: 15px; flex-wrap: wrap;">
                    <a href="#contact" class="btn btn-primary">
                        <i class="fas fa-paper-plane"></i> Hire Me
                    </a>
                    <a href="https://hatemourid.com" target="_blank" class="btn btn-secondary">
                        <i class="fas fa-external-link-alt"></i> Visit hatemourid.com
                    </a>
                </div>
            </div>
        </div>
    </section>

    <!-- Domains Section -->
    <section id="domains" class="domains-section">
        <h2 class="section-title">Domain Portfolio</h2>
        
        <div class="domains-grid">
            <div class="domain-card featured">
                <div class="domain-icon">
                    <i class="fas fa-crown"></i>
                </div>
                <h3>Hate Mourid</h3>
                <a href="https://hatemourid.com" target="_blank" class="domain-url">hatemourid.com</a>
                <p class="domain-description">
                    Primary professional domain featuring AI solutions, 
                    digital services, and my complete portfolio. Central hub 
                    for all my professional activities and client projects.
                </p>
            </div>
            
            <div class="domain-card">
                <div class="domain-icon">
                    <i class="fas fa-brain"></i>
                </div>
                <h3>AI Core Digital</h3>
                <a href="https://aicoredigital.com" target="_blank" class="domain-url">aicoredigital.com</a>
                <p class="domain-description">
                    Advanced AI solutions and digital transformation services. 
                    Specializing in machine learning implementations and 
                    intelligent system development.
                </p>
            </div>
            
            <div class="domain-card">
                <div class="domain-icon">
                    <i class="fas fa-passport"></i>
                </div>
                <h3>Arab Visa Hustle</h3>
                <a href="https://arabvisahustle.com" target="_blank" class="domain-url">arabvisahustle.com</a>
                <p class="domain-description">
                    AI-powered visa processing system. Revolutionizing visa 
                    applications with intelligent automation and real-time 
                    tracking capabilities.
                </p>
            </div>
        </div>
    </section>

    <!-- Skills Section -->
    <section id="skills" class="skills-section">
        <h2 class="section-title" style="text-align: center;">Professional Skills</h2>
        
        <div class="skills-container">
            <div class="skill-card">
                <div class="skill-header">
                    <i class="fas fa-robot"></i>
                    <h3 class="skill-name">AI/ML Data Annotation</h3>
                </div>
                <p style="color: #aaa; margin-bottom: 15px;">Expert in labeling and annotating data for machine learning models</p>
                <div class="skill-level">
                    <div class="skill-progress" data-width="95"></div>
                </div>
            </div>
            
            <div class="skill-card">
                <div class="skill-header">
                    <i class="fas fa-code"></i>
                    <h3 class="skill-name">Web Development</h3>
                </div>
                <p style="color: #aaa; margin-bottom: 15px;">Full-stack development with focus on AI integration</p>
                <div class="skill-level">
                    <div class="skill-progress" data-width="90"></div>
                </div>
            </div>
            
            <div class="skill-card">
                <div class="skill-header">
                    <i class="fas fa-chart-line"></i>
                    <h3 class="skill-name">Data Analysis</h3>
                </div>
                <p style="color: #aaa; margin-bottom: 15px;">Advanced analytics and insight generation from complex datasets</p>
                <div class="skill-level">
                    <div class="skill-progress" data-width="88"></div>
                </div>
            </div>
            
            <div class="skill-card">
                <div class="skill-header">
                    <i class="fas fa-shopping-cart"></i>
                    <h3 class="skill-name">E-commerce</h3>
                </div>
                <p style="color: #aaa; margin-bottom: 15px;">Shopify store development and digital product management</p>
                <div class="skill-level">
                    <div class="skill-progress" data-width="85"></div>
                </div>
            </div>
        </div>
    </section>

    <!-- Stores Section -->
    <section id="stores" class="domains-section" style="background: rgba(10, 14, 23, 0.8);">
        <h2 class="section-title">E-commerce Stores</h2>
        
        <div class="domains-grid">
            <div class="domain-card">
                <div class="domain-icon" style="background: linear-gradient(45deg, #7B68EE, #9370DB);">
                    <i class="fas fa-shopping-bag"></i>
                </div>
                <h3>AI Core Digital Store</h3>
                <a href="https://aicoredigital.myshopify.com" target="_blank" class="domain-url">aicoredigital.myshopify.com</a>
                <p class="domain-description">
                    Shopify store offering AI tools, digital products, 
                    and technology solutions for businesses and developers.
                </p>
            </div>
            
            <div class="domain-card">
                <div class="domain-icon" style="background: linear-gradient(45deg, #FF6B6B, #FF8E8E);">
                    <i class="fas fa-bolt"></i>
                </div>
                <h3>Cheikh Quick Sale</h3>
                <a href="https://cheikhquicksale.com" target="_blank" class="domain-url">cheikhquicksale.com</a>
                <p class="domain-description">
                    Fast-track e-commerce platform specializing in 
                    quick digital sales and instant delivery solutions.
                </p>
            </div>
            
            <div class="domain-card">
                <div class="domain-icon" style="background: linear-gradient(45deg, #00FFAA, #00CC88);">
                    <i class="fas fa-store"></i>
                </div>
                <h3>Ely Store</h3>
                <a href="https://elystore.com" target="_blank" class="domain-url">elystore.com</a>
                <p class="domain-description">
                    Personal brand store featuring curated digital 
                    products, AI tools, and exclusive content.
                </p>
            </div>
        </div>
    </section>

    <!-- AI Chat System -->
    <div id="aiChat" class="ai-chat-container">
        <div class="chat-header">
            <h3><i class="fas fa-robot"></i> AI Assistant</h3>
            <button onclick="closeChat()" style="background: none; border: none; color: white; font-size: 20px; cursor: pointer;">
                <i class="fas fa-times"></i>
            </button>
        </div>
        
        <div class="chat-body" id="chatBody">
            <div class="message ai">
                <p>Hello! I'm the AI Assistant for Ely Cheikh Mourid's portfolio. I can tell you about his skills, projects at <strong>hatemourid.com</strong>, or contact information.</p>
                <small style="color: #aaa; display: block; margin-top: 5px;">Just now</small>
            </div>
        </div>
        
        <div style="padding: 20px; border-top: 1px solid rgba(255,255,255,0.1);">
            <input type="text" id="chatInput" placeholder="Ask about hatemourid.com, skills, or projects..." 
                   style="width: 100%; padding: 15px; border-radius: 25px; border: 1px solid rgba(0,212,255,0.3); background: rgba(0,0,0,0.3); color: white;">
            <button onclick="sendMessage()" style="margin-top: 10px; width: 100%; padding: 15px; border-radius: 25px; background: linear-gradient(45deg, var(--primary), var(--secondary)); border: none; color: white; cursor: pointer;">
                Send Message
            </button>
        </div>
    </div>

    <!-- AI Chat Button -->
    <button class="ai-chat-btn" onclick="toggleChat()">
        <i class="fas fa-robot"></i>
    </button>

    <!-- Footer -->
    <footer id="contact" class="footer">
        <div class="footer-grid">
            <div class="footer-section">
                <h3>Ely Cheikh Mourid</h3>
                <p style="color: #aaa; line-height: 1.6;">
                    AI Data Annotator & Entrepreneur specializing in 
                    intelligent systems and digital solutions. Primary domain: 
                    <strong>hatemourid.com</strong>
                </p>
                <div style="display: flex; gap: 15px; margin-top: 20px;">
                    <a href="#" style="color: var(--primary); font-size: 20px;"><i class="fab fa-linkedin"></i></a>
                    <a href="#" style="color: var(--primary); font-size: 20px;"><i class="fab fa-github"></i></a>
                    <a href="#" style="color: var(--primary); font-size: 20px;"><i class="fab fa-twitter"></i></a>
                </div>
            </div>
            
            <div class="footer-section">
                <h3>Contact Info</h3>
                <ul class="footer-links">
                    <li>
                        <a href="tel:+18044853384">
                            <i class="fas fa-phone"></i> +1 (804) 485-3384
                        </a>
                    </li>
                    <li>
                        <a href="mailto:elycheikhmourid1@gmail.com">
                            <i class="fas fa-envelope"></i> elycheikhmourid1@gmail.com
                        </a>
                    </li>
                    <li>
                        <a href="https://hatemourid.com" target="_blank">
                            <i class="fas fa-globe"></i> hatemourid.com
                        </a>
                    </li>
                </ul>
            </div>
            
            <div class="footer-section">
                <h3>Quick Links</h3>
                <ul class="footer-links">
                    <li><a href="#home"><i class="fas fa-home"></i> Home</a></li>
                    <li><a href="#domains"><i class="fas fa-globe"></i> Domains</a></li>
                    <li><a href="#skills"><i class="fas fa-code"></i> Skills</a></li>
                    <li><a href="#stores"><i class="fas fa-store"></i> Stores</a></li>
                </ul>
            </div>
            
            <div class="footer-section">
                <h3>Business Hours</h3>
                <ul class="footer-links">
                    <li><i class="fas fa-clock"></i> Monday - Friday: 9AM - 6PM</li>
                    <li><i class="fas fa-clock"></i> Saturday: 10AM - 4PM</li>
                    <li><i class="fas fa-clock"></i> Sunday: Emergency Only</li>
                    <li><i class="fas fa-globe"></i> Timezone: EST (UTC-5)</li>
                </ul>
            </div>
        </div>
        
        <div class="copyright">
            <p>© 2024 Ely Cheikh Mourid. All rights reserved. | Primary Domain: <strong>hatemourid.com</strong></p>
            <p style="margin-top: 10px; font-size: 12px;">AI-Powered Portfolio System | Developed with advanced web technologies</p>
        </div>
    </footer>

    <script>
        // ===== INITIALIZATION =====
        document.addEventListener('DOMContentLoaded', function() {
            // Hide loading screen
            setTimeout(() => {
                document.getElementById('loading').style.opacity = '0';
                setTimeout(() => {
                    document.getElementById('loading').style.display = 'none';
                    initializePortfolio();
                }, 500);
            }, 2000);
            
            // Animate skill bars
            animateSkillBars();
        });
        
        function initializePortfolio() {
            console.log('🌟 Portfolio System Initialized');
            console.log('👤 Name: Ely Cheikh Mourid');
            console.log('🌐 Primary Domain: hatemourid.com');
            console.log('📞 Contact: +1 (804) 485-3384');
            console.log('📧 Email: elycheikhmourid1@gmail.com');
            console.log('🚀 Additional Domains: aicoredigital.com, arabvisahustle.com');
            console.log('🛍️ Stores: aicoredigital.myshopify.com, cheikhquicksale.com');
            
            showNotification('Welcome to my AI-Powered Portfolio! Visit hatemourid.com');
        }
        
        function animateSkillBars() {
            const skillBars = document.querySelectorAll('.skill-progress');
            skillBars.forEach(bar => {
                const width = bar.getAttribute('data-width');
                setTimeout(() => {
                    bar.style.width = width + '%';
                }, 500);
            });
        }
        
        // ===== AI CHAT SYSTEM =====
        let chatOpen = false;
        
        function toggleChat() {
            const chat = document.getElementById('aiChat');
            if (chatOpen) {
                chat.style.display = 'none';
                chatOpen = false;
            } else {
                chat.style.display = 'flex';
                chatOpen = true;
                document.getElementById('chatInput').focus();
            }
        }
        
        function closeChat() {
            document.getElementById('aiChat').style.display = 'none';
            chatOpen = false;
        }
        
        function sendMessage() {
            const input = document.getElementById('chatInput');
            const message = input.value.trim();
            
            if (!message) return;
            
            // Add user message
            addChatMessage(message, 'user');
            input.value = '';
            
            // AI response
            setTimeout(() => {
                const response = generateAIResponse(message);
                addChatMessage(response, 'ai');
            }, 800);
        }
        
        function addChatMessage(text, sender) {
            const chatBody = document.getElementById('chatBody');
            const messageDiv = document.createElement('div');
            messageDiv.className = `message ${sender}`;
            
            const time = new Date().toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' });
            
            messageDiv.innerHTML = `
                <p>${text}</p>
                <small style="color: #aaa; display: block; margin-top: 5px;">${time}</small>
            `;
            
            chatBody.appendChild(messageDiv);
            chatBody.scrollTop = chatBody.scrollHeight;
        }
        
        function generateAIResponse(message) {
            const lowerMsg = message.toLowerCase();
            
            const responses = {
                hatemourid: "hatemourid.com is Ely's primary professional domain featuring AI solutions, digital services, and complete portfolio. It's the central hub for all professional activities.",
                skill: "Ely specializes in: AI Data Annotation, Web Development, Data Analysis, E-commerce. He has 95% proficiency in AI/ML annotation.",
                contact: "Contact Ely:\n📞 +1 (804) 485-3384\n📧 elycheikhmourid1@gmail.com\n🌐 Primary domain: hatemourid.com",
                domain: "Ely's domains:\n👑 hatemourid.com (Primary)\n🤖 aicoredigital.com (AI solutions)\n✈️ arabvisahustle.com (visa processing)",
                store: "Ely's stores: aicoredigital.myshopify.com, cheikhquicksale.com, elystore.com - All feature AI-powered systems.",
                project: "Current projects include AI visa processing systems, data annotation platforms, and e-commerce solutions with AI integration.",
                hire: "To hire Ely: Contact via phone/email, visit hatemourid.com, discuss project requirements, agree on timeline and budget. He works remotely globally.",
                default: "I can tell you about hatemourid.com (primary domain), Ely's skills, projects, or contact information. What would you like to know?"
            };
            
            if (lowerMsg.includes('hatemourid') || lowerMsg.includes('hate mourid')) {
                return responses.hatemourid;
            }
            
            for (const [key, response] of Object.entries(responses)) {
                if (lowerMsg.includes(key)) return response;
            }
            
            return responses.default;
        }
        
        function showNotification(message) {
            const notification = document.createElement('div');
            notification.style.cssText = `
                position: fixed;
                top: 100px;
                right: 30px;
                background: linear-gradient(45deg, rgba(0,212,255,0.9), rgba(255,0,128,0.9));
                color: white;
                padding: 15px 25px;
                border-radius: 10px;
                z-index: 9999;
                max-width: 300px;
                animation: slideInRight 0.5s ease, fadeOut 0.5s ease 3s forwards;
                box-shadow: 0 10px 30px rgba(0,0,0,0.3);
            `;
            
            notification.innerHTML = `
                <div style="display: flex; align-items: center; gap: 10px;">
                    <i class="fas fa-user-circle"></i>
                    <div>
                        <strong>Ely Cheikh Mourid</strong>
                        <p style="margin: 5px 0 0 0; font-size: 14px;">${message}</p>
                        <p style="margin: 5px 0 0 0; font-size: 12px; font-weight: bold;">Primary Domain: hatemourid.com</p>
                    </div>
                </div>
            `;
            
            document.body.appendChild(notification);
            
            setTimeout(() => {
                if (notification.parentNode) {
                    notification.parentNode.removeChild(notification);
                }
            }, 3500);
        }
        
        // Add styles for notifications
        const style = document.createElement('style');
        style.textContent = `
            @keyframes slideInRight {
                from { transform: translateX(100%); opacity: 0; }
                to { transform: translateX(0); opacity: 1; }
            }
            @keyframes fadeOut {
                from { opacity: 1; }
                to { opacity: 0; }
            }
        `;
        document.head.appendChild(style);
        
        // Handle Enter key in chat
        document.getElementById('chatInput').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') sendMessage();
        });
        
        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const targetId = this.getAttribute('href');
                if (targetId === '#') return;
                
                const targetElement = document.querySelector(targetId);
                if (targetElement) {
                    window.scrollTo({
                        top: targetElement.offsetTop - 80,
                        behavior: 'smooth'
                    });
                }
            });
        });
        
        // Highlight active nav link on scroll
        window.addEventListener('scroll', function() {
            const sections = document.querySelectorAll('section[id]');
            const navLinks = document.querySelectorAll('.nav-link');
            
            let current = '';
            sections.forEach(section => {
                const sectionTop = section.offsetTop;
                const sectionHeight = section.clientHeight;
                if (scrollY >= (sectionTop - 100)) {
                    current = section.getAttribute('id');
                }
            });
            
            navLinks.forEach(link => {
                link.classList.remove('active');
                if (link.getAttribute('href') === `#${current}`) {
                    link.classList.add('active');
                }
            });
        });
    </script>
</body>
</html>